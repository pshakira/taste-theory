services:
  postgres:
    image: postgres:17
    container_name: taste-theory-db
    environment:
      POSTGRES_DB: taste_theory
      POSTGRES_USER: taste
      POSTGRES_PASSWORD: taste
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taste -d taste_theory"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
