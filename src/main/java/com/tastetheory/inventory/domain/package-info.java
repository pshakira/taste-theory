/**
 * Inventory business rules: items, their purchase and recipe units, pack
 * pricing, yield, and suppliers.
 *
 * <p>Pure Java — no Spring or JPA annotations. Rules belong on the entities and
 * value objects in this package, not in the layer above. If a class here is
 * nothing but getters and setters, the model is wrong.
 */
package com.tastetheory.inventory.domain;
