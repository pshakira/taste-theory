/**
 * Adapters for the shared value objects: JPA attribute converters, JSON
 * serialisers, and the common HTTP error representation.
 *
 * <p>The framework code lives here precisely so that {@code shared.domain} does
 * not have to know that databases or HTTP exist.
 */
package com.tastetheory.shared.infrastructure;
